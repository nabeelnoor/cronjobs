import { ConsoleLogger } from '@nestjs/common';

export class Logger extends ConsoleLogger {
  /**
   * To enable dependancy injection for the configuration
   * To extend the function of Logger
   */
}
